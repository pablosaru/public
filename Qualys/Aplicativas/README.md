```bash
python3 -m venv venv
source venv/bin/activate  # Activa el entorno virtual
pip install -r requirements.txt

export QUALYS_USER=bancg3nd
export QUALYS_PASS=XXXXX
```

Para salir del entorno virtual:

```bash
deactivate
```

```cmd.exe (Windows)
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt

set QUALYS_USER=bancg3nd
set QUALYS_PASS=XXXXX
```